---
"hex-app": minor
---

Add a condensed read-aloud toggle to the Agent voice window: a button next to the mute control switches between reading the full reply and a short spoken summary that the Claude Code hook generates headlessly via 'claude --safe-mode -p' (only while read-aloud is on), keeping the terminal output clean
