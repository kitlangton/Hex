---
"hex-app": patch
---

Add a global "Condensed Read-Aloud" setting. When off (the default) the voice window reads the full reply and the agent is never asked for a summary, so it costs no extra tokens. When on, each turn is condensed to a short summary that's read aloud, and the voice window's per-card toggle flips between the summary and the full reply instantly.
