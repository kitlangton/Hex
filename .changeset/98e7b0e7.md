---
"hex-app": patch
---

Remove preferred provider concept - transformations now strictly use configured providerID from text_transformations.json. Throws error if provider not found instead of falling back.
