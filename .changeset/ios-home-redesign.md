---
"hex-app": minor
---

Redesign the iOS Home screen so its hierarchy matches how the app is used (#P1-3). Dictation — the whole keyboard Flow Session — collapses into one compact header toggle: on starts the session (with a single muted "swipe back" line), off ends it. The arbitrary session countdown and the redundant "Flow session active" / "swipe back" banners are gone, along with the raw model identifier in the status pill. The in-app New note mic becomes the centered hero, and Recent shows hairline rows with the transcript-kind icon. Plain language throughout ("Dictation", not "Flow session").
